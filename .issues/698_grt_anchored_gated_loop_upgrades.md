# Issue 698: GRT-anchored gated loop upgrades (modelless runtime corollaries of arXiv:2608.15062)

**Status:** OPEN — **T1 DONE 2026-08-30** (gain-spectrum bench landed: 54% by loop 2 / 82% by 4 / 99% by 8, gain NOT concave on random weights → `l_min=2` borderline, T4 must read the table); **T2 DONE 2026-08-30** (FixedAnchor shipped + A/B: early speedup TRANSFERS, constant-ρ injection BLOCKS late contraction → T3's closing schedule is the required complement); **T3 DONE 2026-08-30** (convex copy-late schedule shipped + A/B: contraction CONFIRMED 4–12 orders, destination shift 11.9 nats is the price — the honest arbitration); **T4 DONE 2026-08-30** (grt_default l_min=2 + opt-in concavity floor shipped; **the GRT contraction premise REFUTED on the InterLoopNorm step-size axis** — the signal GROWS to the fixed point (13→20, cos θ=1.0), the armed floor cuts at the two-phase knee, verdict: keep disarmed, re-arm only with a decay-capable signal); **T5 DONE 2026-08-30** (CacheStrategy::Mean shipped opt-in + the stash window-forward DELETED: 0.88× end-to-end wall-clock, 0/12 argmax flips + band 2.19e-1 pinned, k=1 degenerate bit-exact both modes; the default flip wants a real-weights quality record — the fixture arbitrates the mechanism, not the deployed path); **T7 GATE PROBED 2026-08-31 — REFUSED** (max separation 1.16× < 2×, inverted at k≥4 — easy tokens gain MORE; see the T7 entry); T6/T8 open (both defer-marked by design). Filed 2026-08-30 from `../.research/519_GRT_Gated_Recurrent_Transformers.md` (No-GD-advocate pass merged; substrate all shipped: Plans 108/136/428/304)

## Problem / opportunity

The GRT paper (5th in the looped-transformer family) supplies measured constants, orderings, and laws that upgrade our shipped loop stack (`forward_looped` + `ResidualGate` + `LoopStabilityMode` + halters) without any training. Three headline facts from the paper:

1. **The loop anchor must be FIXED, not drifting** — trained-gate anchor ablation: frozen prelude output 2.68 vs drifting `h(r-1)` 3.38 (**+0.70 nats**) vs raw embedding 3.73 vs zeros 8.08. Our Plan 428 FLT_res variant distributes the *drifting* prev state; GRT's evidence points at the frozen variant.
2. **The gate should be convex, not additive** — `h(r) = g⊙h(r-1) + (1−g)⊙o` bounds ‖h‖ ≤ max(‖prev‖, ‖out‖) for free; our additive `h̃ + ρ⊙h_prev` has no bound (the exact instability Plan 428 fights). And the gate schedule is transcribable: write-early → copy-late (openness 0.18 → 0.07 monotone; copy-saturated dims 19.7% → 28.8%).
3. **Exit is a quality rule with a floor** — 77% of loss reduction lands in the first 2 steps; per-step gain concave; extending R past training-R *degrades*. Feeds GainCostLoopHalter an `l_min=2` default + concavity assertion, and motivates an offline gain-spectrum table.

## Tasks (priority order — T1 de-risks the rest)

- [x] T1 **Gain-spectrum measurement bench**: for a pinned fixture + existing weights, measure Δloss(r) for r = 0..R → committed lookup table (loop budget → expected quality); asserts monotonicity + records where the gain actually concentrates. G1 bit-identical re-measurement; fixture hash pinned in the same commit. **If our model puts 40% of gain in step 2, the paper's l_min=2 does not transfer — that is a finding, not a failure.**
  **DONE 2026-08-30** (`tests/issue_698_t1_gain_spectrum.rs`, modelless CPU-only, feature `lt2_looped,loop_stability_fix`). Pinned fixture: `Config::micro()` + seed-42 weights (blake3[16] `fab06e3f4ba65977`) + `InterLoopNorm` (the shipped stability fix) + elastic loop override per r; reference = loop-32 output (drift KL(32,40)/loss(1) = 2.6e-9 — sound); quality curve = mean KL(softmax(r) ‖ softmax(32)) over all 27 prompt tokens, r = 1..=24, pinned as raw bits (`PINNED_SPECTRUM`, debug + release bit-identical). **Measured spectrum: 54.0% of total convergence by loop 2 (paper: 77%), 81.6% by loop 4, 99.2% by loop 8** — same front-loaded shape, heavier tail; **verdict BORDERLINE per the pre-registered bands (≥60% would transfer cleanly): T4 should pair `l_min=2` with the table, not adopt it blind.** Honest findings recorded in-test: (1) per-step gain is **NOT concave mid-run** (dips r=3→4 +0.35, jumps r=4→5 +1.39 — two-phase convergence) → the paper's concavity rule does not transfer to random weights; (2) monotone only through r=15, then a ≤4.6e-6 noise-floor plateau (f32 cancellation jitter, asserted as monotone-phase + plateau shape, not strict global monotonicity); (3) KL clamped at 0 (naive f32 sum cancels to −1.4e-8 at the fixed point). Gates: G1 double-run bit-identity + pinned-table exact bits (debug 0.47 s / release 0.01 s), clippy 0 in file, no-regression goat_428 + issue_407 both PASS.
- [x] T2 **`LoopStabilityMode::FixedAnchor`**: hoist `ctx.prev_h` at `tau==0` into a dedicated anchor buffer; inject per-loop. A/B ordering test (fixed vs drifting vs none vs raw-embed) — falsifiable content is the *ordering*, not the absolute numbers. G3 flag-off byte-identical; G4 one hoisted buffer, zero alloc. Caveat honestly recorded: paper numbers are from anchor-trained weights; on vanilla-trained weights the anchor is OOD input — the ordering is the structural claim under test.
  **DONE 2026-08-30** (`tests/issue_698_t2_fixed_anchor.rs` + the FixedAnchor mechanism in `forward_looped`; modelless CPU-only): production mechanism in `forward_looped` (`src/transformer/variants.rs`) — the inter-loop norm is composed INTO the mode (GRT's gate consumes LN inputs: prerequisite, not competitor); the anchor is hoisted ONCE at the end of the tau==0 iteration into `ForwardContext::loop_anchor` (new feature-gated field in `katgpt-forward`, `vec![0.0; n_embd]` in the constructor — zero per-forward alloc); the residual gate's injected state swaps drifting `h^(τ-1)` → frozen anchor. **Interpretant note (deliberate deviation from the literal task text):** the anchor is the first-pass OUTPUT `h^(0)`, not the tau==0 pre-pass `prev_h` — on our prelude-less arch that pre-pass state IS the raw embedding, the paper's DISTINCT worse arm (Table 11: 3.73 vs 2.68); the literal hoist would have collapsed fixed ≡ raw-embed and made the A/B tautological. A/B bench `tests/issue_698_t2_fixed_anchor.rs` (+ `[[test]]` required-features `lt2_looped,loop_stability_fix`): gates are ARMED (`ResidualGate::new_loop_stable`, ρ=0.5 — the zero-init default makes every arm's injection a no-op, the Plan-483 finding); 4 arms, all through production `forward_looped`: **fixed** (FixedAnchor), **drift** (InterLoopNorm — identical conditions, drifting source: the Table-11 comparator), **zeros** (FixedAnchor + zeroed gates — no injection), **none** (un-normed context). **Measured verdict: PARTIAL TRANSFER, with the mechanism found** —
  · Early (r≤4): fixed < drift — 2.93 vs 6.45 at r=2, 0.96 vs 2.01 at r=4: the paper's direction TRANSFERS (asserted + bit-pinned).
  · Late (r≥8): REVERSED — the constant-ρ fixed arm NEVER SETTLES: ref drift KL(32,40) = 2.404 vs 3.9e-3 (drift) / 3.2e-8 (zeros); ρ-sweep monotone and never zero (ρ=0.1 → 2.2e-2, 0.25 → 8.3e-2, 0.5 → 2.404). The paper's late contraction comes from its CLOSING gate schedule (openness 0.18 → 0.07, copy-late) → **T3's schedule is the required complement, not a rival** (a static small ρ is not the fix — it must CLOSE).
  · NoneDefault (un-normed armed) is worst at every r — the Plan-428 norm story re-confirmed under armed gates.
  · Destination bias KL(fixed@32 ‖ drift@32) = 2.56 nats — the anchor moves the fixed point substantially (recorded; fixed@32 unconverged, so this is distance-to-moving-target).
  · Cross-bench consistency proof: the Zeros row EXACTLY equals T1's pinned spectrum at r=2/4/8/16 (zeros = norm + no injection = T1's InterLoopNorm trajectory) — bit-level across two independent benches.
  Gates: G1 double-run bit-identity per arm + full bit-pin table (debug 0.38 s / release 0.01 s bit-identical); a-priori mechanism pins (r=1 arm-independent — anchor never read; zeros-under-FixedAnchor ≡ zeros-under-InterLoopNorm — the mode's ONLY effect is the injected source; fixed ≠ drift vacuity guard; G4 buffer allocated exactly once, `len == capacity == n_embd`); fixture hash == T1's `fab06e3f4ba65977`; G3 no-regression (T1's pinned spectrum reproduces + goat_428 + issue_407 PASS); clippy 0 in touched files (2 pre-existing out-of-class warnings elsewhere are the documented owner-call findings). Honest scope: the raw-embed arm is the p=0 degenerate of our hoist (documented, not run — measuring it would need a second anchor source); the raw-embed-vs-fixed gap (3.73 vs 2.68) is exactly the prelude-transform value our h^(0) hoist captures as fixed-vs-embedding-by-construction.
- [x] T3 **Convex copy-late gate schedule interpretant** for `ResidualGate`: `ResidualGate::copy_late_schedule(loop_count, g0, gR)` + convex-blend application path; free-theorem spec test (‖h(r)‖ ≤ max(‖prev‖, ‖out‖) every loop, the HLA-boundedness class). Form-mismatch caveat: existing checkpoints trained under the additive form — fixture A/B (convex-schedule vs additive vs none) arbitrates; sweep 2–3 schedules, commit the sweep report. Note the direction tension with 414 Fusion B's stability decay (ρ→0.8): on shared weights the fixture decides; paper evidence (convergence by R) supports copy-late.
  **DONE 2026-08-30** (`tests/issue_698_t3_copy_late.rs` + `CopyLateShape` / `ResidualGate.convex_schedule` / `copy_late_schedule[_shaped]` / `convex_gate_at` in `katgpt-types/src/enums.rs` + the convex branch in `forward_looped`'s gate block; feature-neutral mechanism — the branch is data-driven on `convex_schedule: Option<Vec<f32>>`, no new cfg; `[[test]]` gated `lt2_looped,loop_stability_fix`). **Mechanism:** the SCALAR per-loop copy weight g_τ replaces the additive combine with `h^(τ) = g_τ⊙src + (1−g_τ)⊙h̃` (src = anchor under FixedAnchor / prev otherwise) — two free properties the additive form lacks: (1) the exact norm bound ‖h^(τ)‖ ≤ max(‖src‖, ‖h̃^(τ)‖) by norm convexity (per-channel gates forfeit exactness: p=(1,0), o=(0,1), per-channel g=(1,0) → ‖h‖=√2 — the counterexample is pinned in the spec test as WHY-scalar); (2) update magnitude ∝ (1−g_τ) → a copy-late schedule CONTRACTS the loop. Shapes: Linear / EaseOutClose (quadratic, closes fast early) / StepMid (midpoint step); values clamped [0,1]; past-end queries clamp to gR (elastic override running long stays closed). **Measured verdict — CONTRACTION CONFIRMED, with the price found:**
  · Settling (ref drift KL(32,40)): ConvLin 5.754e-6 · ConvEase 5.497e-8 · ConvStep 1.185e-12 (best) · ConvDrift 6.116e-8 · ConvNone 2.752e-4 (no-norm ablation still settles) vs AddConst 2.404 — **4 to 12 orders, asserted at a 10× margin**. T2's open complement CLOSES: harder closure settles better (Step > Ease > Linear; endpoint sweep monotone: (0.3,0.9)→6.4e-5, (0.5,0.95)→5.8e-6, (0.7,0.95)→1.7e-6).
  · **THE PRICE (destination bias, the arbitration number):** KL(ConvLin@32 ‖ natural@32) = **11.90 nats** vs the additive arm's 2.37 (reverse 6.10) — the convex fixed point is ANCHOR-DOMINATED and far from the natural trajectory. On untrained weights the schedule buys contraction + the free bound AT THE COST of relocating the destination; promoting onto real models needs trained gates (riir-train Plan 364) or a quality gate on real weights. The theorem + mechanism are the modelless yield.
  · **StepMid phase finding (honest, pinned):** its closure BEGINS at the midpoint, so the whole measured r-grid [2..16] sits in the open phase — loss(r) is non-monotone BY PHASE STRUCTURE (loss(2)=0.777 < loss(16)=1.470, pinned as an assert) while its tail settles BEST of all arms. Convergence monotonicity is a property of MONOTONE schedules, not of closing per se.
  · ConvLin loss(2) = 0.723 vs AddConst 2.927: at equal loop budget the convex arm is already near ITS (moved) fixed point — early-exit-shaped, consistent with the paper's exit-as-quality-rule, modulo the destination caveat above.
  Gates: G1 double-run bit-identity per arm + full bit-pin table (debug 0.62 s / **release 0.01 s** bit-identical); cross-bench proof: AddConst/NormOnly rows + drifts reproduce T2's committed pins BIT-EXACTLY (0x4019d721 / 0x330a9972) through the restructured gate block; a-priori pins (r=1 arm-independent incl. convex arms; g≡1 frozen-copy → logits(r) bit-identical ∀r≥1 — the closed-end degenerate; convex ≠ additive + anchor ≠ drift vacuity; schedule construction: shapes/monotonicity/clamp/G4-once/`convex_gate_at`); free-theorem spec test (bound grid over 15 case families × 21 g-values + coordinate box + 8-step induction + g=1/g=0 bit-exact degenerates — weight-free, exact); G3 no-regression (T1 + T2 + goat_428 + issue_407 all PASS; default `cargo check` clean); clippy 0 in touched files (2 pre-existing out-of-class warnings elsewhere: percepta legacy len_zero + pruners rule_extractor if_same_then_else, both documented owner-call findings). Honest scope: random weights (the fixture arbitrates — see dest-bias), single-position prompts, micro config; `gates` left EMPTY under convex constructors (the additive path never reads it; documented).
- [x] T4 **Halter floors** (`gain_cost_halt.rs`): `l_min = 2` default + concavity floor rule — gated on T1's measured spectrum; keep the cos-θ oscillation detector armed as the non-convergent fallback (contraction is measured, not proven, for arbitrary weights).
  **DONE 2026-08-30** (`tests/issue_698_t4_halter_floors.rs` + `grt_default` / `with_concavity_floor` / `HaltReason::NonContraction = 2` / `CONCAVITY_NOISE_FRAC` in `crates/katgpt-core/src/gain_cost_halt.rs`; the rule rides the EXISTING per-loop `halt_decision` call — zero `forward_looped` changes; `[[test]]` gated `lt2_looped,loop_stability_fix,gain_cost_halt`). **Mechanism:** `grt_default()` = (τ 1.0, patience 1, **l_min 2**, concavity disarmed) — the GRT floor shipped PAIRED WITH T1's table, NOT as a `Default` flip (the generic `Default` stays l_min=1: its other consumer, the CGSP KnpcSelector planning horizon, has no GRT evidence, and flipping it would be exactly the blind cross-domain adoption T1's gate forbids). The concavity floor is OPT-IN: halt `NonContraction` after `patience` consecutive gain inversions, evaluated LAST (extension guard — fires only when the loop would otherwise continue), inversion counted only above the noise floor (1% of the episode's first gain — plateau jitter is not expansion), streak reset by any non-inverting loop and by the halt itself (episode semantics for KnpcSelector), NaN-safe in the continue direction, seeding THROUGH the l_min floor (refused loops are observations). The oscillation detector is untouched and stays armed under any floor config — precedence pinned (floor → oscillation → scissors → concavity).
  **THE HEADLINE — the GRT contraction premise REFUTED on the production signal axis:** the T4 probe measured the per-loop step size ‖Δh‖ under InterLoopNorm **GROWING to its fixed point**: 13.010 (loop 2) → 17.174 → 19.604 → 19.621 → 19.997 → 20.349 → 20.361 (loop 8), then a 20.2415 ± 4e-4 plateau with **cos θ = 1.0000** for the rest of the run. InterLoopNorm makes convergence DIRECTIONAL (cos θ → 1.0 and T1's KL → 1e-8 both prove it) while the step magnitude tracks the fixed point's residual update — six consecutive above-floor inversions on the ramp alone. Consequences, all pinned in the e2e: (1) the disarmed `grt_default` halter has **NO TRACTION** on the fixture — scissors can't fire (cost 0.130 ≪ plateau 20.24), oscillation can't fire (cos θ ≡ 1.0), so its output is bit-identical to the no-halter baseline; (2) the concavity floor armed at patience 2 halts at **loops 4–6 per token (27/27 early; 21×4 + 6×6, bit-pinned)** — cutting the run exactly at the two-phase knee T1 measured; the halt ≡ elastic equivalence holds per token (halted state bit-identical to the same-token elastic-r run); (3) never-halt control (l_min=255) bit-identical to baseline. **Verdict: the concavity floor must stay DISARMED on the InterLoopNorm step-size axis; re-arming requires a decay-capable signal (KL / erank quality gain) — a companion wiring question, not a floor change.** The kernel rule itself ships as the reproducible artifact (contract pinned by synthetic unit tests; the refutation is an e2e pin, the bench-760-T3 class).
  Gates: kernel 39/39 under `gain_cost_halt` (11 new T4 tests incl. grt_default config-equivalence, noise-floor jitter immunity, l_min seeding-through, oscillation precedence, post-halt episode reset, NaN paths, scissors precedence; struct-size guard 20 → 40 bytes documented); e2e G1 double-run bit-identity **debug + release** (0.09 s / 0.01 s); fixture hash == T1's `fab06e3f4ba65977`; G3 no-regression — T1 + T2 + T3 + goat_428 + issue_407 ALL PASS through the kernel changes, katgpt-core lib 2017/0 (feature-on), the Plan-304 bench GOAT matrix re-run ALL PASS (G2 76.7% / G3 / G4) with the new enum variant wired into its 4 exhaustive matches; clippy 0 in touched files (bench had pre-existing rustfmt drift at HEAD — the +4-line diff is match arms only per the drift rule); default `cargo check` clean; probe patch fully reverted (`src/` diff empty).
- [x] T5 **KV mean-across-steps**: `CacheStrategy::Mean` running mean of K/V across loop iterations (fixed loop order = deterministic f32 sum). **Lossy-surface promotion rule in full**: gate on argmax stability + max_abs band (the Bench-773 lesson — max_rel with a floor denominator cannot certify lossy numerics), never bit-identity; per-family conditional retention on the deployed path. tf_loop side: replaces the dedicated stash pass with the loop's own running mean — a whole window-forward deleted (measure end-to-end wall-clock).
  **DONE 2026-08-30** (`tests/issue_698_t5_kv_mean.rs` + `CacheStrategy::Mean` in `katgpt-types/src/looping.rs` + `tf_kv_mean_{k,v}` buffers in `katgpt-forward` (n_layer × kvd each, tf_loop-gated, layer-major rows) + the fold hooks in both `IterationMode` arms + the stash-block `Mean` arm in `src/transformer/tf_loop.rs`; `[[test]]` gated `tf_loop`). **Mechanism:** each loop iteration's freshly written K/V rows at `pos` fold into a per-layer incremental streaming mean `mean ← mean + (fresh − mean)/count` (count 1-based, fixed order ⇒ deterministic f32 sum; count==1 is the plain copy — also the staleness reset); the stash block's `Mean` arm writes the accumulated rows back and **deletes the dedicated stash window-forward entirely** (post-loop consumption follows the First shape: ctx.x stays the blended state). `k==0` degenerates to First semantics (nothing to mean over). The fold is behind a read-once `mean_kv` flag — First/Last paths structurally unchanged (the `tf_stash_x` save moved INTO the First arm, bit-identical). **Measured verdict — ALL GATES PASS, promotion-grade:**
  · **G1**: every arm's decode bit-identical across repeat runs (logits + cache rows); debug + release band bits identical (`0x3e5fd968`).
  · **k=1 degenerate EXACT** (the plumbing pin): with β=0 the single iteration's mean IS that iteration's K/V == the First stash's K/V, so Mean ≡ First **bit-for-bit end-to-end** — logits AND every cache K/V row across the whole decode, BOTH iteration modes.
  · **Lossy gate (k=4, β=0.5, 12-position decode): 0/12 argmax flips** (per-family conditional retention = 100%) + max_abs band **2.186028e-1** pinned as raw bits (fixture blake3[16] `23d0daab3f087159`, micro + n_layer=3, window (1,1) — a real 3-phase forward).
  · **Recorded finding — Mean is 14× closer to the First canonical KV than Last is**: Last vs First flips 6/12 argmaxes at band 3.07e0, vs Mean's 0/12 at 2.19e-1. The running mean (early iterations dominate, sub-stepping evolves from the pre-window state) stays near the First canonical row while the Last stash (final blended state) lands far from it.
  · **Wall-clock: 0.881× debug / 0.884× release** (min-of-5 rounds × 400 decodes × 16 positions, reused ctx+cache — alloc-free steady state). Theoretical floor 5/6 ≈ 0.833 is window-ONLY; the measured end-to-end ratio implies the shared non-window work (pre/post-loop, embedding, lm_head, attention over growing context) ≈ 2.6× the 1-layer window cost on this fixture — consistent with theory.
  · **Promotion decision (the delegated perf/sec call): `CacheStrategy::Mean` ships OPT-IN, NOT as the `TrainingFreeLoopConfig` default.** All gates passed, but the default flip is a lossy-surface behavior change on a public API whose deployed-path quality evidence is a random-weights micro fixture — the same class as T1's BORDERLINE verdict: the mechanism + gates are the modelless yield; the default flip wants a real-weights quality record (the lossy-surface promotion rule's per-family conditional retention ON THE DEPLOYED PATH, not just the fixture). Re-arm: run the band + argmax gate on a real checkpoint, then flip `default`/`from_config` in one commit.
  Gates: fixture-hash + band-bit pins (same-platform determinism; cross-platform drift → relax + record, never re-base silently); G3 T1+T2+T3+T4+goat_428+issue_407+test_136 ALL PASS through the kernel changes (T1's pinned spectrum reproduces bit-exactly); katgpt-types 130 lib tests; clippy 0 in touched files (the 2 pre-existing out-of-class warnings elsewhere are the documented owner-call findings); rustfmt clean on all 4 touched files; default `cargo check` clean. Honest scope: random weights, 3-layer fixture, single-token decode schedule; the attention-read path inside the loop is UNCHANGED (each iteration still attends its own fresh K/V — only the cache state left for FUTURE positions is the mean).
- [ ] T6 **Per-step state noise** (deferred, afternoon-cost): BLAKE3-seeded per-(pos, loop) Gaussian on the loop input; expect a wash (+0.018 nats is the paper's smallest ablation, measured on noise-trained weights).
- [-] T7 **Per-token difficulty routing** (deferred, gated): only if a T1-side probe shows ≥2× marginal-gain separation by step-1 entropy/margin; per-token branching breaks batched prefill — decode-only first. PABEE-class prior art [unverified — quota] covers the mechanism; our content is the separation measurement.
  **GATE PROBE DONE 2026-08-31 — REFUSED** (`tests/issue_698_t7_gate_probe.rs`, `[[test]] lt2_looped,loop_stability_fix`, modelless CPU-only; reuses the T1 harness verbatim and cross-checks T1's pinned spectrum bits — the per-token-KL means reproduce within 2.5e-7 rel on x86_64-windows; the fixture blake3 hash itself drifts off-aarch64 (weight-init libm ulp → different weight BYTES, measured `c894478d3febdb00` vs T1's `fab06e3f4ba65977`) — recorded, not asserted, per T1's documented cross-platform escape hatch; the gate verdict is a same-platform ratio so ulp noise cannot move it). **Measured: max finite separation 1.160× < 2× (margin @ k=2), and INVERTED at the meaningful budgets — by entropy: 0.900× @ k=2, 0.335× @ k=4, 0.393× @ k=8; by margin: 1.160× @ k=2, 0.475× @ k=4, 0.503× @ k=8.** The step-1 difficulty signals ANTI-correlate with marginal loop gain on this fixture: spearman(entropy, G(4)) = −0.350, spearman(margin, G(4)) = +0.281, spearman(entropy, kl(1)) = −0.527 — uncertain-at-step-1 tokens are far from ANY fixed point, not from their own (the model stays genuinely diffuse about them at r=32), while confident tokens are the ones still converging. Recorded context: 4/27 tokens carry negative G(2) — per-token trajectories wobble at k=2 even though the token-mean is monotone (T1's two-phase structure at per-token granularity); extreme-5 strata agree with the tertiles (0.410–0.415× @ k=4). **T7 stays deferred. Reopen triggers: anchor-trained (non-random) weights — the paper's regime, where the difficulty signal may decouple from fixed-point diffuseness; or a difficulty signal that predicts loop-gain POSITIVELY at ≥2× (margin's direction is already right, +0.281, it just does not clear the bar).** The refusal is itself the routing finding: an entropy-gated early-exit on this fixture would cut the WRONG tokens' loops (entropy-stratified separation < 1 at k≥4 — the easy tokens are the ones still gaining).
- [ ] T8 **Hand conditional gate** `σ(β·(cos(h, h_pre)−θ)+b)` (deferred, coin-flip): mechanism direction (open on divergence) extracted from the paper's contrastive projection finding; honest coin-flip on weights that never learned contrastive reading; requires T1 mechanism gate (gate-open events co-locate with high marginal gain, rank correlation threshold) + T2 anchor + inter-loop norm prerequisite.

## Dependencies

- T2/T3/T8 compose with Plan 428's `InterLoopNorm` (GRT's gate consumes LN inputs — normalization is a prerequisite, not a competitor); composition ablated in order: norm → anchor → gate schedule.
- Training-side companion lives in `riir-train/.plans/364` (depth sampling, gate-init recipes, GRT-micro arms) — do not duplicate here; this issue is runtime/modelless only.

## References

- `../.research/519_GRT_Gated_Recurrent_Transformers.md` — full distillation, Path-0 table, signal-diff vs our `ResidualGate`
- arXiv:2608.15062v4 (Hegazy, Alanwar, Elhoushi); ablations §5 + Appendix B/E; anchor ablation Table 11
- Plans 108 (LT2 loop), 136 (training-free loop / damped Euler), 428 (loop-stability PoC, `LoopStabilityMode`), 304 (GainCost halting); Research 414 (loop-stability gap table)
